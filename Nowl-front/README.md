# 河北工程大学校园服务前端

前端项目（Vue 3 + TypeScript + Vite）。

## 启动

```bash
npm install
npm run dev
```

## 打包

```bash
npm run build
```

## 环境变量

参考 `.env.example`，常用项：

- `VITE_API_BASE_URL`：后端 API 基础路径（开发环境建议 `/api`）
